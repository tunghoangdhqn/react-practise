const initState = {
  todos: [],
  todoInput: ''
};

function reducer(state, action) {
  switch (action.type) {
    case "":

      break;

    default:
      break;
  }
}

export { initState };
export default reducer;